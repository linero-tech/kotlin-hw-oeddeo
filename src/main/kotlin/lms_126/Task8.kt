package lms_126

fun task8(): String {

    return """
        if x is multiple of both 3 and 5
            output "Foobar"
        else
        if x is multiple of 3 but not 5
            output "Foo"
        else
        if x is multiple of 5 but not 3
            output "Bar"
        else
        if x is not multiple of 3 or 5
            output "Qix"
        
        
    """.trimIndent()
}
